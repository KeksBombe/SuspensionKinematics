#pragma once

namespace suspkin {

/// How the mesh is drawn. Solid is the plain shaded surface; Triangles adds the
/// tessellation on top, which is what you look at to judge an import.
enum class DisplayMode { Solid, Triangles };

} // namespace suspkin
